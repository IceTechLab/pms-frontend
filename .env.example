# Base URL of the Express API for production builds (no trailing slash).
# Leave unset in development - Vite proxies /api to http://localhost:3001.
# On Vercel, set this in Project Settings -> Environment Variables, e.g.:
# VITE_API_URL=https://pms-backend.onrender.com
VITE_API_URL=
